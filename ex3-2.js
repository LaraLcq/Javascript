let values = [1, 2, 3, 4, 5];
let count = values.reduce((previous, current) => current += previous);
let avg = count / values.length;

console.log(avg);


let arr = [100, 101, 102];
let sum = arr.reduce((previous, current) => current += previous);
let avgi = sum / arr.length;

console.log(avgi);


